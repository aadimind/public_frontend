VITE_API_BASE_URL=https://api.micromath.example.com
# Mock data is opt-in. The frontend falls back to mock data only in development
# when this is unset or "true". In production, set "false" and configure a real API.
VITE_USE_MOCK_DATA=true
VITE_SITE_URL=https://micromath.example.com